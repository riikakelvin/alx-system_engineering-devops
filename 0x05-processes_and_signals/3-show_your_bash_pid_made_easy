#!/usr/bin/env bash
# This script displays the PID, along with the process name, of processes whose name contain the word bash.

PID = $(pgrep -f bash)
for PID in $PID;
	do
		process = $(pgrep -l -P $pid)
	       	echo "PID: $PID and the Process Name: $process"
	done
